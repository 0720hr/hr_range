<group name="local,syslog,sshd,pam,su,">

  <!-- Overwrite the built-in brute-force-then-success rule to add SSH/T1110
       context and raise severity. Carries $(srcip) so the attacker address appears in the alert. -->
  <rule id="40112" level="13" timeframe="240" overwrite="yes">
    <if_group>authentication_success</if_group>
    <if_matched_group>authentication_failures</if_matched_group>
    <same_source_ip />
    <description>Compromise: successful login from $(srcip) after a brute-force from the same IP (T1110/T1078).</description>
    <mitre><id>T1110</id><id>T1078</id></mitre>
    <group>authentication_success,attack,</group>
  </rule>

  <rule id="100004" level="13" timeframe="600">
    <if_sid>5501</if_sid>
    <match>opened for user root</match>
    <if_matched_sid>5763</if_matched_sid>
    <description>CRITICAL: successful su-to-root by a brute-forced account - full host compromise (T1548/T1078).</description>
    <mitre><id>T1548</id><id>T1078</id></mitre>
    <group>authentication_success,attack,privilege_escalation,</group>
  </rule>

  <rule id="100003" level="12" timeframe="600">
    <if_sid>5301</if_sid>
    <if_matched_sid>5763</if_matched_sid>
    <description>Privilege escalation attempt: failed su-to-root by a recently brute-forced account (T1548).</description>
    <mitre><id>T1548</id></mitre>
    <group>authentication_failed,attack,privilege_escalation,</group>
  </rule>

</group>

<group name="sysmon,windows,powershell,">
  <rule id="100100" level="12">
    <if_sid>61603</if_sid>
    <field name="win.eventdata.commandLine" type="pcre2">(?i)-enc</field>
    <description>PowerShell executed with an encoded command on $(win.system.computer)</description>
    <mitre>
      <id>T1059.001</id>
      <id>T1027</id>
    </mitre>
  </rule>
</group>
<group name="sysmon,windows,discovery,">
  <rule id="100101" level="8">
    <if_sid>61603</if_sid>
    <field name="win.eventdata.originalFileName" type="pcre2">(?i)^whoami\.exe$</field>
    <description>System/User Discovery: whoami executed on $(win.system.computer) (T1033)</description>
    <mitre>
      <id>T1033</id>
    </mitre>
  </rule>
  <rule id="100102" level="6">
    <if_sid>61603</if_sid>
    <field name="win.eventdata.originalFileName" type="pcre2">(?i)^sysinfo\.exe$</field>
    <description>System Information Discovery: systeminfo executed on $(win.system.computer) (T1082)</description>
    <mitre>
      <id>T1082</id>
    </mitre>
  </rule>
</group>
<group name="sysmon,windows,persistence,">
  <rule id="100103" level="8">
    <if_sid>61603</if_sid>
    <field name="win.eventdata.originalFileName" type="pcre2">(?i)^schtasks\.exe$</field>
    <field name="win.eventdata.commandLine" type="pcre2">(?i)/create</field>
    <description>Scheduled task created via schtasks on $(win.system.computer) (T1053.005)</description>
    <mitre>
      <id>T1053.005</id>
    </mitre>
  </rule>
</group>
